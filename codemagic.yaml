workflows:
  android-workflow:
    name: Android APK
    instance_type: mac_mini_m2
    scripts:
      - flutter pub get
      - flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
